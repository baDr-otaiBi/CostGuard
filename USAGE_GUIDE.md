# دليل الاستخدام - CostGuard AI
# Usage Guide - CostGuard AI

<div dir="rtl">

## 🚀 البدء السريع

### 1. التثبيت

```bash
# استنساخ المشروع
git clone https://github.com/baDr-otaiBi/CostGuard.git
cd CostGuard

# تثبيت المتطلبات
pip install -r requirements.txt
```

### 2. التشغيل الأساسي

```bash
python Financial.py
```

سيقوم النظام بتوليد بيانات تجريبية وتحليلها تلقائياً.

## 📊 فهم المخرجات

### الملفات المولدة

#### 1. البيانات الخام
- **salla_data_full.csv** - بيانات المعاملات الكاملة (3000 طلب)

#### 2. التقارير
- **analysis_summary_ai.json** - ملخص التحليل مع نتائج AI

#### 3. الرسوم البيانية (10 رسوم)

**التحليلات التقليدية (8 رسوم):**
- `chart_1_heatmap.png` - خريطة حرارية للمبيعات حسب الساعة واليوم
- `chart_2_abc_analysis.png` - تحليل ABC (منتجات A/B/C)
- `chart_3_volume_profitability.png` - مصفوفة الحجم والربحية
- `chart_4_monthly_trends.png` - الاتجاه الشهري للمبيعات
- `chart_5_category_comparison.png` - مقارنة الفئات
- `chart_6_margin_distribution.png` - توزيع هوامش الربح
- `chart_7_geographic_distribution.png` - التوزيع الجغرافي
- `chart_8_daily_sales.png` - نمط المبيعات اليومي

**تحليلات الذكاء الاصطناعي (2+ رسوم):**
- `ai_sales_forecast.png` - التنبؤ بالمبيعات لـ 30 يوم
- `ai_customer_segments.png` - توزيع مجموعات العملاء

#### 4. السجلات
- **costguard_ai.log** - سجل العمليات والأخطاء

## 🤖 فهم نتائج الذكاء الاصطناعي

### 1. التنبؤ بالمبيعات

```
✅ تم تدريب نموذج التنبؤ بدقة: 76.06%
📈 متوسط الخطأ المطلق: 1,305 ريال
🔮 المبيعات المتوقعة للشهر القادم: 110,267 ريال
```

**التفسير:**
- الدقة 76% تعني أن النموذج يتنبأ بشكل صحيح في 76% من الحالات
- المبيعات المتوقعة تساعد في التخطيط للمخزون والموارد

### 2. تجميع العملاء

```
✅ تم تجميع 7 عميل إلى 3 مجموعات
📊 عملاء VIP - أفضل العملاء: 1 عميل
📊 عملاء متوسطون - إمكانية النمو: 4 عميل
📊 عملاء جدد - يحتاجون رعاية: 2 عميل
```

**التفسير:**
- **VIP**: عملاء عاليو القيمة - يحتاجون رعاية خاصة وعروض حصرية
- **متوسطون**: إمكانية تحويلهم إلى VIP بالاستراتيجيات الصحيحة
- **جدد**: يحتاجون برامج ترحيب وتعريف بالمنتجات

### 3. كشف الحالات الشاذة

```
✅ تم فحص 3000 معاملة
⚠️ تم اكتشاف 297 حالة شاذة (9.90%)
📊 متوسط قيمة الحالات الشاذة: 2,556 ريال
💰 إجمالي الحالات الشاذة: 759,110 ريال
```

**التفسير:**
- الحالات الشاذة قد تكون طلبات ضخمة أو معاملات مشبوهة
- تحتاج للمراجعة لضمان عدم وجود أخطاء أو احتيال

### 4. تحسين الأسعار

```
✅ تم تحليل أسعار 15 منتج
💡 عدد المنتجات التي تحتاج تعديل: 4
```

**التوصيات المحتملة:**
- زيادة السعر للمنتجات ذات الهامش المنخفض
- تخفيض السعر للمنتجات عالية الهامش ومنخفضة المبيعات
- الاحتفاظ بالسعر للمنتجات المثالية

### 5. توصيات المنتجات

```
✅ تم توليد 13 توصية ذكية
```

**الاستخدام:**
- عرض المنتجات المقترحة في صفحات المنتجات
- إنشاء حزم منتجات تكميلية
- تحسين تجربة البحث والاكتشاف

### 6. تحسين المخزون

```
✅ تم تحليل 15 منتج
📊 منتجات سريعة الحركة: 4
```

**التصنيفات:**
- **سريع الحركة**: زيادة المخزون والتركيز التسويقي
- **متوسط الحركة**: المحافظة على المستوى الحالي
- **بطيء الحركة**: تخفيض المخزون أو عروض تصريف

## 📖 استخدام الإعدادات

يمكنك تخصيص النظام عبر ملف `config.json`:

```json
{
  "ai_features": {
    "enabled": true,
    "models": {
      "forecasting": {
        "days_to_predict": 30  // غيّر لتنبؤ أطول/أقصر
      }
    }
  }
}
```

## 🔧 الاستخدام المتقدم

### تحليل بيانات حقيقية

1. استبدل دالة `generate_advanced_salla_data()` بتحميل بياناتك:

```python
def load_real_data():
    df = pd.read_csv('your_salla_data.csv')
    # تأكد من توافق أسماء الأعمدة
    return df
```

2. عدّل `main()` لاستخدام بياناتك:

```python
def main():
    df = load_real_data()  # بدلاً من generate_advanced_salla_data()
    # ... بقية الكود
```

### تعطيل ميزات AI

إذا واجهتك مشاكل في تثبيت scikit-learn:

1. حذف/تعليق سطر التثبيت من requirements.txt
2. سيعمل النظام تلقائياً بدون ميزات AI
3. ستحصل على التحليلات التقليدية فقط

## 💡 نصائح وأفضل الممارسات

### 1. دقة التنبؤات
- كلما زادت البيانات التاريخية، زادت دقة التنبؤات
- الموسمية تؤثر بشكل كبير على الدقة
- راقب معدل الخطأ (MAE) لتقييم جودة التنبؤ

### 2. تجميع العملاء
- راجع المجموعات بشكل شهري
- استخدم النتائج لتخصيص الحملات التسويقية
- ركز على تحويل المجموعات المتوسطة إلى VIP

### 3. الحالات الشاذة
- افحص الحالات الكبيرة يدوياً
- قد تكشف عن فرص أو مشاكل
- استخدمها لتحسين آليات الرقابة

### 4. تحسين الأسعار
- لا تطبق كل التوصيات مرة واحدة
- اختبر التعديلات على عينة صغيرة أولاً
- راقب تأثير التغييرات على المبيعات

## 📊 قراءة التقرير JSON

```python
import json

with open('analysis_summary_ai.json', 'r', encoding='utf-8') as f:
    data = json.load(f)

# الوصول للبيانات
total_revenue = data['analysis_summary']['total_revenue']
ai_forecast = data['ai_insights']['forecasting']['total_predicted']
anomalies = data['ai_insights']['anomalies']['total_anomalies']

print(f"الإيرادات: {total_revenue:,.0f} ريال")
print(f"التنبؤ: {ai_forecast:,.0f} ريال")
print(f"حالات شاذة: {anomalies}")
```

## 🐛 استكشاف الأخطاء

### خطأ: "ModuleNotFoundError: No module named 'sklearn'"

**الحل:**
```bash
pip install scikit-learn
```

### خطأ: "Memory Error"

**الحل:**
- قلل عدد الطلبات في `config.json`
- استخدم عينة من البيانات الحقيقية

### التحذير: "ML_NOT_AVAILABLE"

**السبب:** مكتبات AI غير مثبتة

**الحل:** سيعمل النظام بدون AI، أو قم بتثبيت المكتبات

## 📞 الدعم والمساعدة

- **Issues**: افتح Issue في GitHub
- **Documentation**: راجع README.md
- **Logs**: تحقق من ملف costguard_ai.log

</div>

---

**Made with ❤️ by بدر العتيبي**
